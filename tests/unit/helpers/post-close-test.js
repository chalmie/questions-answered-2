import { postClose } from 'questions-answered/helpers/post-close';
import { module, test } from 'qunit';

module('Unit | Helper | post close');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = postClose([42]);
  assert.ok(result);
});
